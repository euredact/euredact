{
  "name": "euredact",
  "version": "0.3.1",
  "description": "European PII redaction SDK -- fast, zero-dependency, rule-based detection and redaction of personal data across 31 European countries",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/esm/index.d.ts",
        "default": "./dist/esm/index.js"
      },
      "require": {
        "types": "./dist/esm/index.d.ts",
        "default": "./dist/cjs/index.js"
      }
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": [
    "pii",
    "redaction",
    "gdpr",
    "privacy",
    "europe",
    "eu",
    "iban",
    "bsn",
    "national-id",
    "phone",
    "email",
    "anonymization",
    "data-protection",
    "personal-data",
    "regex"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/euredact/euredact.git"
  },
  "scripts": {
    "build:esm": "tsc",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "postbuild:cjs": "echo '{\"type\":\"commonjs\"}' > dist/cjs/package.json",
    "build": "rm -rf dist && npm run build:esm && npm run build:cjs",
    "test": "npm run test:spec && npm run test:inference && npm run test:conformance",
    "benchmark": "node --import tsx src/__tests__/benchmark.ts",
    "prepublishOnly": "npm run build",
    "test:spec": "node --import tsx src/__tests__/spec.ts",
    "test:eval": "node --import tsx src/__tests__/run.ts",
    "test:eval:full": "node --max-old-space-size=8192 --import tsx src/__tests__/evalFull.ts",
    "test:conformance": "node --import tsx src/__tests__/conformance.ts",
    "test:inference": "node --import tsx src/__tests__/inference.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0"
  }
}
